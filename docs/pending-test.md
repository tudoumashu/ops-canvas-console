# 待测试

- Local Workspace v1 Phase 8 已新增自动化回归覆盖：`opsc serve` state 目录权限、`bearer.token`/`launch.secret`/session 文件权限、CLI `serve` 普通输出和 JSON 输出脱敏、错误响应脱敏、带 Origin 的 bearer 拒绝、browser session runtime/workspace 脱敏、AI proxy 只使用 profile `secretRef` 且不把浏览器 Authorization/cookie/local headers 转发给模型供应商、MCP `tools/list` 工具面冻结、workspace info/doctor/export plan/GC dry-run/run status/run events/index rebuild stdio smoke、本地模板草稿 run 创建、固定素材复制为 canonical artifact、run artifact ref、node state、event 和 artifact 文件读取。
- Phase 8 仍需人工回归：用真实浏览器启动 `opsc serve --workspace ~/OpsCanvas --origin <Web origin>`，用一次性 `launch.secret` 建立 session，确认浏览器只保存 loopback `baseUrl`；用真实 OpenAI-compatible 测试 key 走本地 profile 拉模型并发起一次低成本文本/图片请求；在真实 Codex / Claude Code MCP client 中配置 `opsc mcp --workspace <真实 workspace>`，确认客户端侧能发现并调用只读/诊断工具；在 Web UI 用本地模板固定素材启动一次 local run，确认状态页 artifact 预览来自 `/api/local/artifacts/<art_id>/files/original`。旧浏览器测试数据和现有 PDD/VPS run 明确不迁移。
- `opsc mcp` 首版 MCP stdio wrapper 已有协议级自动化 smoke；仍需要在真实 agent 客户端中回归：构建 `opsc` 二进制后，把 Codex / Claude Code 的 MCP server command 指向该二进制并传 `["mcp", "--workspace", "<真实 workspace>"]`，确认 `tools/list` 能看到 `opsc_workspace_info`、`opsc_run_status` 等工具，客户端 UI 中看不到 profile/project/asset/prompt/template/run/artifact 的 create/update/delete 类对象 mutation 工具；调用 workspace info、doctor、template/run/artifact/profile/project/asset/prompt list、run status/events、export plan、GC dry-run 和 index rebuild 时，默认输出不泄露 workspace 绝对路径、project `rootPath`、bearer token、launch secret、serve URL 或 secret 明文；未启动同一 workspace 的 `opsc serve` 时 `opsc_workspace_index_rebuild` 应作为 tool error 返回。
- Web UI 启动时会清理旧浏览器私有事实源 key：需要在测试浏览器手动写入 `infinite-canvas:ai_config_store`、`infinite-canvas:asset_store`、`infinite-canvas:prompt_store`、`infinite-canvas:canvas_store`、`text_generation_logs`、`image_generation_logs`、`video_generation_logs` 和 `ops-canvas-workflow-folders` 到 localforage `app_state` 或 `localStorage`，刷新后确认这些 legacy keys 被移除；同时确认当前 `opsc:*_cache:v1` 展示缓存、local workspace loopback `baseUrl`、生成中/上传中/保存失败需要的 `image_files/media_files` 临时媒体桥接不被启动清理误删。还需要手动写入旧版 `opsc:local_workspace_connection`，其中包含 `workspace/runtime/tokenFile/launchSecretFile` 等字段，刷新后确认该 key 迁移为只包含 `state.baseUrl` 和版本号，不保留 workspace/runtime metadata。
- Web UI 本地工作区可用性检测需要回归：未启动 `opsc serve` 时，顶部本地工作区弹窗和“我的素材 / 我的提示词 / 画布库 / 工作台 / 我的工作流”等本地私有页面应明确提示“未检测到 opsc serve”；启动 `opsc serve` 但未输入 launch secret 时，应显示“服务已启动，等待授权”一类提示；输入错误或已消费的 launch secret 后状态不得停留在 `checking`；输入正确 launch secret 后页面应变为 connected，浏览器 `localStorage` 仍只保存 loopback `baseUrl`。
- Web UI local workspace adapter 已把顶部本地工作区连接入口、`我的素材`、`我的提示词`、画布项目库、画布项目内图片/视频媒体、画布导入/导出 zip 媒体文件、工作台 text/image/video 生成记录和 AI 本地 profile/proxy 接到 `opsc serve`：需要启动 `OPENAI_API_KEY=<测试 key> opsc serve --workspace ~/OpsCanvas --origin <当前 Web origin>`，用 runtime state 中的一次性 `launch.secret` 建立 session，确认浏览器 `localStorage` 不保存 token/launch secret，也不再保留旧 `infinite-canvas:ai_config_store` 中的 API Key；在配置弹窗选择本地直连，填写 Base URL 和 SecretRef 环境变量名后，profile 写入 `profiles/<profile_id>/profile.json` 且只包含 `secretRef`，使用非默认 env var 名时刷新页面或重新连接本地工作区后仍应回显同一个 env var 名，不应回退到 `OPENAI_API_KEY`，拉取模型和一次文本/图片/视频请求都应通过 `/api/local/ai/v1/*` 代理完成；素材/提示词列表、创建、编辑、删除、公共库复制到我的、画布/工作台/PDD 创作画布“存素材”、素材选择器插入都通过 local workspace 生效；图片/视频导入后文件出现在 workspace `assets/<asset_id>/files/`，提示词正文出现在 `prompts/<prompt_id>/content.md`；素材/提示词浏览器 cache 不应持久化私有列表或正文，切换不同 workspace 时不得短暂显示上一个 workspace 的素材/提示词；导出“我的素材”zip 应从当前 workspace 文件端点打包图片/视频文件，`assets.json` 不应依赖旧 browser `storageKey` 才能带出文件；导入“我的素材”zip 应通过 `opsc serve` 写入 `assets/<asset_id>/` 和 `files/original`，不得把包内图片/视频恢复到浏览器 `image_files/media_files` 作为事实源；新增、更新或删除素材成功后，当前素材列表和画布项目都不再引用的 `image:*` / `video:*` / `file:*` browser blob 应被清理，仍被画布引用的 storageKey 不得提前删除；画布库创建、重命名、删除、打开、导入后应写入 `canvas-projects/<canvas_id>/canvas-project.json`，画布图片/视频节点和助手媒体应写入 `canvas-projects/<canvas_id>/files/` 并在 JSON 中以 `workspaceFileKey` 引用，刷新、清空浏览器旧 `image_files/media_files` 后仍能从 `opsc serve` 回显；保存成功后，已写入 workspace 且当前画布状态不再引用的 `image:*` / `video:*` / `file:*` browser blob 应被清理，若保存期间继续编辑并仍引用同一 storageKey 则不得提前删除；导出含 `workspaceFileKey` 媒体的画布 zip 后，在清空浏览器临时媒体缓存的情况下重新导入，应在新 `canvas-projects/<canvas_id>/files/` 重新生成文件并正常预览，且导入过程使用的临时 `image:import_*` / `file:import_*` 浏览器 blob 应在导入后清理；分别在文本/图片/视频工作台生成一次记录，确认记录写入 `workbench-logs/<wblog_id>/workbench-log.json`，图片/视频及参考素材文件写入 `workbench-logs/<wblog_id>/files/`，图片/视频当前结果卡片和参考图在保存成功后切换为从 `/api/local/workbench-logs/<wblog_id>/files/<media_key>` 回显，刷新后仍从 `opsc serve` 回显，保存成功、新会话、移除参考图或切换历史记录时对应浏览器 `image:*` 参考图 blob 应被清理，删除记录会删除对应 workspace 记录目录；断开本地工作区时页面应提示未连接且不把浏览器缓存当事实源；旧 `infinite-canvas:asset_store`、`infinite-canvas:prompt_store`、`infinite-canvas:canvas_store`、`text_generation_logs`、`image_generation_logs`、`video_generation_logs` 浏览器测试数据不应自动迁移；生成中、上传参考图、保存失败或保存期间用户切换参考图的临时 Blob 仍需确认只作为临时状态使用。
- Web UI 本地 AI profile 需要额外回归 workspace 切换边界：连接 A workspace 并保存非默认 `SecretRef` 后，再断开或连接一个没有 profile 的 B workspace，配置弹窗应清空为默认本地配置，不应继续显示或使用 A workspace 的 Base URL、模型列表、默认模型或 `SecretRef`；再次连接 A workspace 应从 `profiles/<profile_id>/profile.json` 重新回显。
- 本地工作区弹窗新增“本地项目”面板：连接 `opsc serve` 后，确认列表通过 `/api/local/projects` 展示项目 id、能力、kind、adapter 和 `rootFingerprint`，不展示本机 `rootPath`；新建项目会写入 `projects/<proj_id>/project.json`，能力字段包含 `fs.read`、`fs.write`、`process.exec`、`network.local`、`artifact.write`；编辑项目时才通过 `showPaths=1` 读取并回显本机路径，保存后由后端重新校验绝对路径并计算 fingerprint；删除只删除 workspace 项目引用，不删除外部项目文件；包含 `credentialRef` 的项目应提示用 CLI 修改，Web UI 不应把脱敏摘要写回覆盖真实 secretRef。
- 工作流入口自定义文件夹已从旧 `ops-canvas-workflow-folders` localStorage 迁到 `opsc serve` workspace preferences：连接本地工作区后，在 `/workflows` 新建自定义工作流文件夹应写入 `opsc-workspace.json.data.preferences.workflowFolders`，刷新页面后仍回显；断开本地工作区时新建应提示先连接本地工作区，不得写入旧 localStorage key；并确认 preferences update 的 revision 冲突不会覆盖其它客户端修改。
- Web UI 电商工作流私有模板已接入 `opsc serve` local templates：连接本地工作区后打开 `/workflows/ecommerce/templates`，新建、复制、编辑、保存和删除模板应写入/删除 workspace `templates/<tpl_id>/template.json`，刷新页面后仍从本地 workspace 回显；未连接本地工作区时继续走现有服务器模板和管理员登录；本地模板的“运行模板”只应创建 local run 草稿并在存在固定素材时写入本地 artifact ref，不能把 local template 误提交到现有 VPS run。
- Web UI 电商本地模板的 `material_lookup` 固定素材选择已改为读取当前 local workspace 的 `我的素材` 图片：连接本地工作区后，在模板编辑器选择“素材库查找 -> 固定选择一个素材”应只列出 `assets/<asset_id>/` 中的本地图片素材，保存后 `template.json` 中的 `extra.assetId` 应为本地 asset id；断开本地工作区后服务器模板仍走现有 admin asset 接口。当前真实 local executor 尚未接入，因此不应期待本地 asset id 可被现有 VPS executor 直接执行。
- Web UI 电商工作流本地 run/artifact 基础路径已接入 `opsc serve`：连接本地工作区后，在模板编辑页或 `/workflows/ecommerce` 启动本地模板，应创建 `runs/<run_id>/run.json`、节点状态和 `run.waiting_for_executor` 事件，并跳转到本地 run 状态页；如果模板包含固定 `material_lookup` 本地图片素材，启动时应把 `assets/<asset_id>/files/original` 复制成 `artifacts/<art_id>/files/original`，写入 `runs/<run_id>/artifacts/<art_id>.ref.json`，对应素材节点标记为 `success`，其它未执行节点保持 pending；`/workflows/ecommerce` 列表应同时显示 local run tag、事件数和 artifact 数；本地 run 状态页应能展示 run summary、节点状态、events、artifact refs，并能通过 `/api/local/artifacts/<art_id>/files/original` 预览固定素材 artifact。当前仍没有真实 PDD/VPS executor，因此不应期待自动执行模型节点或生成新产物。
- Local Workspace v1 `opsc serve` 新增本机 loopback HTTP API：默认 `127.0.0.1:17680`，支持 `--port 0`、workspace 外 XDG state runtime metadata、HTTP `bearer.token`、browser 一次性 `launch.secret` + HttpOnly session、CORS local origin 白名单、`{code,data,msg}` 响应、workspace 查询/doctor/index rebuild/export plan/GC plan、run events/SSE、artifact/asset/workbench-log 文件读取、prompt content 读取、`/api/local/ai/v1/*` 本地 AI proxy，以及 profiles/projects/assets/prompts/canvas-projects/workbench-logs 查询与写入 API。已覆盖单元测试，后续在真实个人 workspace 接入 Web UI 前，需要手工确认 state 文件权限、非授权请求拒绝、带 Origin 的 bearer 请求被拒绝、browser session bootstrap 可用、`/api/health` 不泄露路径或 token、浏览器 origin 配置正确，本地 AI proxy 不转发浏览器 Authorization/cookie 且只使用 profile `secretRef`，并且 Web UI 不直接读写 workspace 文件。MCP 首版为 stdio CLI wrapper，真实 agent 客户端回归见本文件顶部 `opsc mcp` 条目。
- Local Workspace v1 foundation 补齐 project root salted fingerprint、project path capability guard、`secretRef` 脱敏 summary、`workspace gc plan` dry-run 和 doctor 对 project credential/fingerprint/GC 规则的检查；`opsc` 新增 `workspace gc plan` 查询命令。已覆盖单元测试，后续在真实个人 workspace 上需手工确认 `project list` 只输出 opaque `rootFingerprint`、`gc plan` 只返回相对路径且不删除文件、`.opsc/project-root.salt` 不进入 export plan。
- Local Workspace v1 foundation 新增 profiles/projects/assets/prompts file-backed repository、对应 `index.sqlite` 增量更新/扫描重建、`workspace doctor` 私有边界检查和 `workspace export plan` 默认排除规则；`opsc` 新增 `profile list`、`project list`、`asset list`、`prompt list`、`workspace export plan` 查询命令。已覆盖单元测试，后续在真实个人 workspace 接入前仍需用非测试目录手工确认默认不输出 secrets、project `rootPath` 或敏感绝对路径，且 export plan 只输出相对路径。
- Local Workspace v1 foundation 新增 templates/runs/artifacts file-backed repository、run events JSONL/follow、run node state、run artifact ref 和 `index.sqlite` 增量更新/扫描重建；`opsc` 新增 `workspace index rebuild`、`template list`、`run list/status/events`、`artifact list` 查询命令。已覆盖单元测试，后续在真实个人 workspace 接入前仍需用非测试目录手工跑一次 `workspace init/info/doctor/index rebuild` 和空列表命令，确认默认不输出 secrets 或敏感绝对路径。
- 电商工作流运行页“创作画布”修复轮询覆盖本地交互导致的抖动和工具栏异常：缩放/平移时不应再因后台刷新跳回，编辑面板切换模型后等待多轮刷新仍保持选择，替换图片/视频后旧内容不应残留，锁比例/自由比例应按原生画布语义拉伸和恢复比例，裁剪确认和多角度生成应有 loading 反馈且生成节点不消失、不触发布局大幅重排。旧保存画布中纯 run 派生媒体节点若因真实尺寸变大互相遮挡，应在刷新后自动留出间距。需要在 VPS 上打开一个 custom run 回归这些交互，并确认刷新页面后创作画布保存结果仍正确。
- 电商工作流运行页“创作画布”本轮继续补齐原生画布交互：小地图改为紧凑节点标识，避免大图节点遮挡其它节点；新增/生成/裁剪/多角度节点会按预期节点外框寻找空位，减少重叠；节点悬浮工具栏沿用原生画布延迟隐藏逻辑，鼠标从节点移动到工具栏时应能稳定点击，顶部空间不足的大媒体节点会把工具栏夹在视口内。工具栏已接入信息、删除、文本编辑、文本生图、图片/视频替换、下载、存素材、锁比例、裁剪、多角度、查看大图、失败重试、编辑面板和“应用副本并重跑下游”。需要在 VPS 上用已完成 custom run 回归：刷新后小地图不互相盖住、工具栏不闪退、每个按钮行为符合原生画布预期、artifact 点击预览仍正常、应用副本后原模板下游节点会重跑。
- PDD 自定义工作流同步本地节点失败时不再表现为一直 running：`sync_local` 反向 SSH 通道不可用会在脚本节点错误中显示明确提示；自定义 run 若已无 running 商品但存在失败商品，运行页顶部应收敛为 `error`。需要在未建立 `-R 22222:127.0.0.1:22` 时跑一次包含同步节点的模板，确认页面、节点日志和最近错误都能看到“本地同步通道不可用”类提示。
- 电商工作流运行页“创作画布”继续对齐原生画布：结果页移除了左上角说明气泡，节点连线恢复为原生贝塞尔曲线；图片/视频产物会以带背景和边框的节点呈现，并按实际媒体宽高适配节点尺寸。需要在 VPS 上打开一个 custom run，确认刷新后节点位置、连线、背景模式、图片信息开关和 artifact 点击预览都正常，且上传/替换的图片或视频仍保存到 `logs/creative_canvas/products/<product>/canvas.json` 和对应 run 目录。
- PDD 工作流模板的 `text_generation`、`image_generation`、`image_edit`、`video_generation` 节点新增“失败重试”配置，默认启用、重试次数 `0` 表示一直重试、间隔秒数 `0` 表示系统退避；内置质检/修复的瞬时重试也改为同一语义，不再默认固定 100 次。需要在 VPS 模板编辑器中确认新建和旧模板节点都能显示/保存该配置，并用一次可控失败的模型调用确认日志中按配置重试。
- `PDD 工作流` 前台入口已改名为 `电商工作流`，并新增 `/workflows/ecommerce`、`/workflows/ecommerce/<run_id>`、`/workflows/ecommerce/templates` 路由别名；运行页新增 `总览视图 / 商品流程 / 创作画布` 三视图。`商品流程` 保持只读流程查看，不再在大图预览中展示人工编辑入口；`创作画布` 会从单个商品产物初始化图片/视频/文本内容节点，并保存到 `runs/<run_id>/logs/creative_canvas/products/<product>/canvas.json`。需要在 VPS 上打开一个已完成 custom run，确认三视图切换、创作画布拖动节点/连线/保存后刷新回显正常。
- 提示词中心公共“提示词库”已重建：系统托管远程提示词会先清空再导入，并在入库层强制归为 `图片 / 通用 / gpt-image-2`，`文本` 和 `视频` 分类不再显示这些图片生成提示词；需要在 VPS 页面确认三个分类数量和筛选结果正确。
- 工作流模板画布和 PDD 自定义运行结果页已移除之前同步自原生画布的节点悬浮工具栏，避免与当前工作流节点配置、结果详情和图片预览交互冲突；需要确认选中节点、打开详情、预览图片和人工编辑副本入口仍正常。
- 我的工作台已同步 Flow2API 图片/视频模型参数适配：切换到 `Nano Banana Pro`、`Nano Banana 2`、`Imagen 4`、`Veo 3.1 Lite/Fast/Quality` 后，应展示 Flow2API 对应的尺寸、质量、帧/素材/续写、秒数和分辨率配置，而不是 GPT 系列默认参数；需要分别用图片和视频模式各发起 1 次测试请求。
- Flow2API 模型选择已增加友好映射层：前端只展示 `Nano Banana Pro`、`Nano Banana 2`、`Imagen 4`、`Veo 3.1 Lite/Fast/Quality` 等通用名称，后端会按图片尺寸/质量或视频文本/帧/素材/续写、横竖屏、秒数、分辨率映射到 Flow2API 真实模型名；需要在“我的工作台”和工作流模板图片/视频节点各测试 1 次，确认生成请求能按参数落到预期模型。
- PDD 工作流模板新增独立 `同步本地/可选上传` 脚本节点，默认链路变为 `产物打包 -> 同步本地/可选上传`；节点默认调用 `scripts/trigger_local_receive_and_upload.sh --run-id {{runId}}`，追加 `--upload` 后可同步后本地上传。需要在本地反向 SSH `-R 22222:127.0.0.1:22` 已建立时跑 1 个商品，确认本地 `runs/<run_id>/generated` 和 `待上架` 被拉回，开启 `--upload` 后本地上传脚本被调用。
- 提示词中心修复公共提示词卡片底部操作区：管理员在“提示词库”中应能看到并点击“编辑/删除”；远程导入提示词统一归为“图片/通用”，选择“文本”或“视频”分类时不应再出现这批图片生成提示词。
- PDD 自定义工作流图片节点内置质检/修复修复了 guardrail 子目录未创建导致 `generation_01/output_01.png` 写入失败的问题；需要在 VPS 上用 1-2 个商品真实跑通源图节点并确认 `logs/custom_workflow/products/*/nodes/source/guardrail/generation_01/output_01.png` 正常落盘。
- 系统设置兼容旧版单行 `global` 设置数据：需要确认旧数据库仍能读取出 `public/private` 模型渠道配置，自定义工作流节点可正常选择 `gpt-image-2`、`gpt-5.5` 和 Flow2API 模型。
- 视频工作台通过 Flow2API 生成的视频已从内存临时缓存改为落盘到 `VIDEO_STORAGE_ROOT`，默认 `data/video`；需要在 VPS 上生成 1 个视频后重启容器，确认 `/api/v1/videos/<id>` 和 `/api/v1/videos/<id>/content` 仍可访问、预览和下载。
- PDD 自定义工作流运行页新增“人工编辑副本”：点击节点图片大图后可输入编辑要求，用当前图片生成一个副本，并可选择应用到当前节点后重跑该商品后续节点。需要在一个已完成 custom run 上选择源图或 mockup 产物测试“仅保存副本”和“应用并重跑后续流程”，确认 `manual_edits/`、节点 `status.json`、`generated/`、`待上架/` 和运行页刷新正常。
- PDD 工作流模板画布工具栏已合并为文本、图片、视频、功能四类；节点具体功能改为在右侧“节点功能”中选择，图片节点内包含素材库查找/图片选择/图片生成/图片编辑，功能节点包含输入/条件/脚本；从节点圆点拖到空白处会弹出引用生成菜单并自动创建连线。需要在 VPS 页面确认四类按钮、节点功能切换、模型按文本/图片/视频过滤、内置质检/修复模型下拉、拖拽创建下游节点和旧模板保存回显正常。
- 素材入口重构为“素材中心”：顶部导航、画布工具栏和生图/视频工作台统一打开素材中心；页面分为“我的素材”（当前事实源为 local workspace，可新增/编辑/删除/导出）和“素材库”（服务器素材）。管理员可在前台素材库新增、编辑、删除服务器素材，普通用户只读、下载、插入画布、复制到我的素材。需要分别用管理员和普通用户确认权限、复制到本地、素材库筛选和 PDD `material_lookup` 匹配。
- 提示词库和素材库新增结构化分类字段归一化：旧记录会在服务启动后的首次列表请求中补齐领域、阶段、模型、输入输出类型、素材用途、分类路径等字段，避免筛选时只显示当前页归一化结果。需要在 VPS 页面确认提示词库按领域/阶段筛选、素材库按用途/分类筛选都能返回结果。
- PDD 模板工作流图片节点运行时参数规范化已对齐原生画布：`quality=auto` 或非法质量不再传给模型，`1k/2k/4k` 会映射为 `low/medium/high`，比例尺寸会按有效质量换算为像素尺寸，图片张数上限调整为 15。需要用模板图片生成/编辑节点分别测试 `auto/auto`、`high/1:1`、`2k/2:3` 和像素尺寸直传。
- 合并上游 v0.1.0：画布项目导出改为下载 `.zip` 压缩包，包内包含 `projects.json` 和当前画布引用到的本地图片、视频文件，避免只导出 JSON 时丢失媒体内容。
- 合并上游 v0.1.0：画布库支持多选后一键导出多个画布项目，导出的压缩包可一次恢复多个项目。
- 合并上游 v0.1.0：画布项目导入改为读取新版 `.zip` 压缩包，会先按 `projects.json` 中的文件映射恢复图片、视频到本地存储，再插入画布项目，导入成功后仍停留在画布库。
- 合并上游 v0.1.0：修复删除画布图片节点或清空画布后撤销时，节点信息恢复但本地图片数据已被清理导致图片丢失的问题。
- 合并上游 v0.1.0：“我的素材”新增导出素材和导入素材入口，可将本地素材导出为包含 `assets.json` 与图片、视频文件的压缩包，并从压缩包恢复素材；PDD 服务器侧素材仍只读展示，不写入本地导出包。
- 合并上游 v0.1.0：生图工作台和画布节点的图片参数区复用紧凑版图像设置面板，尺寸、质量、生成张数交互保持一致；工作台仍保留独立模型选择。
- 合并上游 v0.1.0 的生图工作台记录持久化已被 local workspace adapter 覆盖：当前生成记录 canonical data 应写入 `workbench-logs/`，旧 `storageKey` 记录只作为不迁移的浏览器测试数据。
- 合并上游 v0.1.0：视频设置抽成画布和视频创作台共用紧凑面板，清晰度、尺寸、秒数按固定网格选择并支持手动输入；尺寸选择 `auto` 时请求不传 `size`。
- 合并上游 v0.1.0：修复画布和生图工作台选择图片尺寸后，请求图片生成/编辑接口未携带 `size` 参数的问题；`auto` 不传，其余比例或像素尺寸会随请求发送。
- 合并上游 v0.1.0：画布视频设置浮层改为挂载到页面根层级并使用自建浮层交互，避免被节点悬浮工具栏遮挡或点击面板内容时关闭。
- 合并上游 v0.1.0：画布生成配置节点的生图参数改为复用图像设置浮层，支持在同一个入口里调整质量、尺寸和生成张数。
- PDD 工作流模板新增 v2 无代码模板和复合图片节点：图片节点可内置质检/修复/重生，minor 只记录，major/critical 触发修复，瞬时模型错误使用节点配置的失败重试语义；模板编辑器新增简单/高级模式，简单模式隐藏条件/脚本/图片选择等底层节点，prompt 和输出路径支持变量插入器。需要在 VPS 上创建 `PDD 商品主图模板 v2`，用 1-2 个商品真实跑通，确认 `guardrail/` 内部轮次、`status.json.internal_steps`、业务目录和运行页展示正常。
- PDD 工作流模板列表新增“新建复杂模板 / 新建简化模板”入口；简化模板保留素材、源图、Mockup、最终主图和产物打包，去掉质检/修复/标题生成。两套模板的 Image 2 节点参数对齐 PDD 远程 chatgpt2api 配置：源图/源图修复 `2:3`，Mockup/最终主图/主图修复 `1:1 high`。需要确认模板列表可分别创建两类模板，VPS 当前复杂模板和新增简化模板参数正确。
- PDD 工作流模板流程图视图继续优化：组合节点展开后会在原位显示带背景的容器，子节点限制在容器内并可通过标题栏收起；流程线新增端点偏移和上/下轨道路由，降低主线、辅助输入线和修复回路线重合。需要在浏览器确认两个质量关卡可反复展开/收起、容器背景易区分、连线不再大面积重叠，并确认自由画布模式不受影响。
- PDD 工作流模板编辑器新增“自由画布 / 流程图视图”切换：复杂模板默认用流程图视图展示，质检/判定/修复循环折叠为组合节点，连线改为按主线、参考输入、通过分支和修复回环区分的正交流程线；切回自由画布后仍保留原有拖拽、连线和保存坐标能力。VPS 上 `PDD 商品主图模板` 已把源图、质检、修复、标题、主图等 prompt 更新为基于 PDD 生产 prompt 的 3D 白床单链路文本，需要在浏览器确认流程图清晰、组合节点展开/收起正常，并用 1 个角色跑通模板。
- PDD 工作流模板新增 `condition`、`script` 操作，`text_generation` 支持 `extra.outputFormat=json` 作为结构化输出；连线支持 `fromHandle`、JSONPath 条件和受控循环；脚本节点支持 VPS 执行和本地 agent 执行，本地 agent 通过 `LOCAL_AGENT_TOKEN` 领取任务并运行仓库相对路径脚本。需要在 VPS 上用一个“文字生成 JSON -> 条件分支 -> 修复循环”的小模板验证分流、循环上限、脚本日志和 local agent 任务回传。
- PDD 工作流模板工具栏改为直接创建操作节点：输入、素材库、静态文字、文字生成、条件分支、脚本执行、图片生成、图片编辑、视频生成；独立 `json_generation` 已合并为 `文字生成 + 输出格式 JSON`。需要在浏览器中确认工具栏按钮、旧模板自动迁移和 JSON 输出节点保存后正常。
- 修复生图工作台和画布生图请求中 `quality` 参数可能传入上游不支持值导致 400 的问题；请求前会归一化质量枚举，`auto` 或异常值不再发送给上游。
- 管理后台新增/编辑渠道时，渠道可用模型支持通过弹窗按“新获取、已有”分组选择，并可在弹窗内手动增加模型或拉取模型列表后再写回表单。
- 管理后台编辑渠道时，API Key 留空不再触发必填校验，表示沿用已保存的密钥；新增渠道仍要求填写 API Key。
- 管理后台公开配置里的系统可用模型候选项改为由已启用渠道中选择的模型合并去重生成，最终开放哪些模型仍由公开配置里手动勾选。
- 视频生成请求参数对齐 `grok-imagine-video` 接口：使用 `resolution_name`、`preset=normal`、`input_reference[]`，支持清晰度、尺寸、秒数快捷选择和手动输入，并支持最多 7 张参考图。
- 画布视频设置浮层改为挂载到页面根层级，避免被节点悬浮工具栏遮挡。
- 视频清晰度输入框改为只输入数字，提交请求时再拼接 `p` 单位。
- 视频生成前端会识别后端 `{ code, msg }` 错误响应，创建失败不再继续轮询 `undefined`。
- 新增 `/video` 视频创作台页面，参考生图工作台布局，支持提示词、参考图、视频参数、生成结果、保存素材、下载和本地生成记录；清晰度和秒数均支持常用值选择与手动输入。
- 视频创作台生成前会把模型、尺寸、秒数、清晰度归一化为视频接口支持的参数，并展示后端返回的错误信息，避免页面侧残留的生图参数影响视频请求。
- PDD 自定义工作流运行页继续补齐原生画布交互：顶部 Run 状态会在 `completed` 或所有节点/商品成功时兜底显示 `success`，商品图片缩略图只打开大图不再弹出详情抽屉，大图预览支持滚轮缩放和拖拽平移且不影响背后画布，模型节点摘要新增 operation/model/count/size/quality/prompt/输出路径等配置展示，左下角新增原生小地图和缩放控件。需要在 VPS 页面确认 `custom_20260525_095148` 顶部状态、图片预览关闭/拖拽、节点详情和小地图缩放都正常。
- PDD 自定义工作流本轮修复：图片预览改为自定义 lightbox，点击图片外区域或 Esc 可关闭，滚轮只缩放预览图；自定义 run 内部节点产物改写到 `logs/custom_workflow` 并新增 `logs/custom_workflow.log`，默认模板源图输出到 `generated/<主题 - 人物>/0001.png`，mockup/规格图输出到 `待上架/<标题>/规格图/1_规格图.png`，主图输出到 `待上架/<标题>/主图/1_主图.png`；PDD 启动入口改为先选模板再填 JSON/JSONL 输入，新增 `/workflows` 工作流文件夹页。需要在 VPS 上用两行 JSONL 跑 1 次小任务，确认目录结构、实时日志、运行中旋转图标/耗时、节点统计和图片预览交互。
- PDD Run 运行页已重构为单一主画布：总览视图展示阶段 DAG，商品视图展示所选商品节点，商品列表、详情和实时日志改为右侧抽屉，底部常驻居中工具栏承载视图切换、刷新、续跑、停止和 VPS 运维；商品详情现在与总览一样定时刷新，避免上方阶段已推进但商品画布仍停留在旧节点状态。需要在 VPS 页面确认运行中 custom run 的节点状态同步、商品切换、图片点击放大、抽屉开关和底部工具栏位置。
- PDD 工作流模板运行页已改为按模板快照动态展示 Run Overview 和商品详情节点，并支持多行 JSON 对象输入；需要在 VPS 页面确认两行 `{"theme":...}` JSONL 能启动 2 个商品，运行页节点与模板节点/连线一致，节点输出图片和状态 JSON 可在详情面板查看。
- PDD 工作流模板新增 Image 2 mockup 中间节点：`sku_artwork.psd` 已提取为控制台固定规格图模板素材，默认模板链路调整为 `源图生成 + 规格图模板素材 -> Mockup生成 -> 最终主图`；需要真实跑 1 个商品确认 Image 2 能按两张参考图生成可用 mockup。
- 素材中心和提示词中心筛选体系已改为“媒体分类 + 用途 + 来源 + 自由标签”：素材中心左侧只保留全部/图片/文本/视频，服务器旧 `mockup_base` 会归一到 `规格图模板/spec_template`，提示词中心新增“我的提示词”分区且当前事实源为 local workspace；需要在 VPS 页面确认多标签 AND 筛选、来源筛选、公共提示词加入我的提示词、素材库编辑项和前台展示都正常。
- PDD 工作流模板节点配置面板已按节点操作区分显示：素材/输入节点隐藏无意义模型和 prompt，静态文字节点改为“文本内容”，生成节点保留模型和参数选择；底部工具栏改为视口常驻居中，需要确认保存后回显和工具栏位置正常。
- PDD 工作流模板画布已对齐原生无限画布核心交互：缩放、节点右侧新增、圆点拖拽有向连线、选中节点后显示右侧配置、Delete 删除节点已在 VPS 页面烟测通过；仍需要用户在浏览器中确认小地图、框选、多选、复制粘贴、撤销重做、节点缩放、模板保存和刷新后结构保持。
- PDD 工作流模板画布新增 `/workflows/pdd/templates` 和后端自定义 DAG 执行器：需要在 VPS 上确认模板创建、保存、复制、删除、拖拽节点、配置有向连线、导入 JSON 输入、素材库参考图匹配、文本/图片/视频节点真实模型调用、输出路径映射到 `generated/` 和 `待上架/`，以及 run 状态和商品级 `pipeline_status.json` 正常。
- PDD 工作流新增 `/workflows/pdd` 和 `/workflows/pdd/<run_id>`，后端直接读取 VPS `/opt/pdd-workflow/runs`，支持阶段 DAG、商品详情画布、图片/JSON/log 详情面板和 allowlist 运维动作；需要在 VPS Docker 部署后确认 SSH 隧道访问、管理员登录、run 解析、图片预览、日志流、续跑/停止和服务运维动作都正常。
- PDD 工作流启动任务新增 `console_workflow_spec.json`：主题可手输或导入 JSON，源图/最终主图 Image 2 prompt 和数量可在页面配置，单阶段单商品上限 10 张；VPS dry-run 已确认 `--console-spec`、prompt-only 源图和 `logs/console_artifact_graph.json` 正常，仍需要一次真实 Image 2 小批量 run 确认实际出图、可选参考图和多个最终主图文件。
- PDD 工作流启动时只把 `/opt/pdd-workflow/materials` 导入为服务器侧“素材库”图片，PDD 生产提示词不再自动导入提示词库；需要确认素材中心“素材库”分区能通过通用分类、用途、来源和自由标签搜索/预览这些服务器素材，工作流素材匹配仍能按结构化用途找到参考图。
- 配置弹窗和后台公开配置新增默认视频模型；视频节点通过设置弹层单独配置视频质量、尺寸和秒数，连接图片节点时会作为 `input_reference` 参考图生成视频，并按 OpenAI 视频接口格式提交 `multipart/form-data`、`size`、`seconds`、`vquality` 和参考图参数，需要确认本地直连和云端渠道下默认视频模型选择、节点视频参数配置、参考图生成、保存和重试都正常。
- 画布新增视频节点：工具栏可新建视频节点，上传/拖拽本地视频后可在节点内播放、下载、保存到我的素材；节点下方对话框和生成配置节点支持视频生成，前端会调用 OpenAI 兼容 `videos` 接口并把生成结果保存为本地视频 Blob；后端远程渠道新增 `/api/v1/videos`、`/api/v1/videos/:id`、`/api/v1/videos/:id/content` 代理，需要确认本地渠道和远程渠道都能生成、轮询和下载视频。
- 图片和视频节点会按素材或生成结果的真实宽高比调整节点尺寸；空图片/视频节点在尺寸设置切换到 16:9、9:16 等比例时也会同步调整节点外框，需要确认上传、生成、重试、素材插入和空节点尺寸切换后的黑边情况。
- 模型选择器改为共享的 shadcn 风格下拉，按模型名称显示 OpenAI、Claude、Gemini 图标，并把画布节点下方、右侧助手和配置弹窗统一到同一套组件；右侧助手的模型和模式按钮会随工具栏宽度自动在文字态和图标态之间切换，需要确认生图页、画布助手、节点配置和配置弹窗中的选中态和下拉列表显示正常。
- 画布图像设置里的尺寸输入去掉了原生数字步进箭头，需要确认宽高仍可直接输入、禁用态正常、设置值回写正常。
- 后台提示词定时同步默认改为开启且默认每 5 分钟执行一次，需要确认新环境未保存过设置时开关默认打开、Cron 默认值正确，用户手动关闭或修改 Cron 后能保持配置。
- 画布右侧助手和节点下方对话框的生图设置弹层改为紧凑图像设置面板，面板只包含质量、尺寸、宽高比和生成数量选择，模型选择保留在对话框底部工具栏；需要确认助手内设置面板不会被右侧面板裁切，节点内质量可正常选择并参与生成、尺寸可编辑、宽高比预览显示正常且为直角矩形、auto 不重复显示、张数每行 4 个且自定义输入框能显示当前值、实际生图参数正常；节点设置面板打开时会隐藏节点悬浮工具条，避免遮挡质量选择。
- 管理后台 `/admin/prompts` 新增提示词批量删除，需要确认多选、确认弹窗、删除后列表刷新和筛选条件下删除行为。
- 管理后台 `/admin/settings` 的私有配置新增提示词定时同步开关和 Cron 表达式；开启后后端会按配置同步内置 GitHub 远程提示词源，需要确认保存配置和到点同步行为。
- 管理后台 `/admin/settings` 中的渠道模型列表获取和模型测试已改为走后端接口，前端不再直接读取或转发 API Key，需要确认保存后、编辑中和已有渠道三种场景都能正常拉取模型和测试连通性。
- 提示词远程源新增 `davidwuw0811-boop/awesome-gpt-image2-prompts`，同步时会读取仓库 `prompts.json` 的结构化数据，需要确认后台同步后分类、封面图、标签和提示词内容展示正常。
